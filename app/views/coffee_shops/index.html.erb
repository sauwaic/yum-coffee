<div class="wrapper text-center">
  <%= image_tag 'yum_coffee.png' %>
</div>


<div class="wrapper-list">
  <ul>
    <% @coffee_shops.each do |coffee_shop| %>
    <li> <%= link_to coffee_shop.name, coffee_shop_path(coffee_shop), class: 'list-font' %> </li>
    <% end %>
  </ul>
</div>
